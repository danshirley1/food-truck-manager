{
  "name": "food-truck-manager",
  "version": "1.0.0",
  "description": "Food Truck Manager - Portfolio showcase web application",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "cd web && yarn dev",
    "build": "cd web && yarn build",
    "start": "cd web && npm start",
    "lint": "cd web && yarn lint",
    "test": "cd web && npm test",
    "heroku-postbuild": "cd web && npm install --include=dev && npm run build",
    "deploy:heroku": "git -c http.postBuffer=524288000 push food-truck main:main",
    "deploy:check": "node scripts/deploy-preflight.mjs",
    "deploy": "npm run deploy:check && npm test && npm run build && npm run deploy:heroku"
  },
  "keywords": [
    "food-truck",
    "game",
    "simulation",
    "typescript",
    "nextjs",
    "portfolio"
  ],
  "author": "Food Truck Manager Team",
  "license": "MIT"
}