/** Max sum of |money| + |reputation| + |energy| deltas applied in one turn. */
export const MAX_CUMULATIVE_TURN_DELTA = 30;

export const DIFFICULTY_MAX_EFFECT = {
  early: 10,
  mid: 15,
  late: 20,
} as const;

export const MAX_IMPACT_PER_CHOICE = 35;
